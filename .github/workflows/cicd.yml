name: Build and Push to ECR

on:
  push:
    
jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

        #    - name: Configure AWS credentials
        # run: |
        #   aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
        #   aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        #   aws configure set default.region ${{ secrets.AWS_REGION }}

        # - name: Login to Amazon ECR 
        #   run: |
        #     aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 299372338272.dkr.ecr.us-east-1.amazonaws.com

        # - name: Build and push docker image to Amazon ECR
        #   run: |
        #     docker build -t mediplus:v1 .
        #     docker tag mediplus:v1 299372338272.dkr.ecr.us-east-1.amazonaws.com/mediplus:v1
        #     docker push 299372338272.dkr.ecr.us-east-1.amazonaws.com/mediplus:v1

